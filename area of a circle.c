#include <stdio.h>

int main() {
    
   float r,Area;
    float pi;
    printf("Enter the value of r:");
    scanf("%f",&r);
    printf("Enter the value of pi:");
    scanf("%f",&pi);
    Area=pi*r*r;
    printf("Area of circle value=%.3f",Area);
}
    
