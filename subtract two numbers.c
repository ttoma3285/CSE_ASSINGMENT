#include <stdio.h>

int main() {
 float a,b,subtract;
    printf("enter the value of a:");
    scanf("%f",&a);
    printf("enter the value if b:");
    scanf("%f",&b);
    subtract=a-b;
    printf("The subtract of two value:%.3f",subtract);
    
    return 0;
}
